local UserInputService = game:GetService("UserInputService")
local player = game.Players.LocalPlayer

-- СОЗДАНИЕ GUI
local sg = Instance.new("ScreenGui", game:GetService("CoreGui"))
sg.Name = "AkbarshoxFix"
sg.ResetOnSpawn = false

local frame = Instance.new("Frame", sg)
frame.Size = UDim2.new(0, 250, 0, 280)
frame.Position = UDim2.new(0.5, -125, 0.5, -140)
frame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
frame.Active = true
frame.Draggable = true

local corner = Instance.new("UICorner", frame)
corner.CornerRadius = UDim(0, 12)

-- ЗАГОЛОВОК
local title = Instance.new("TextLabel", frame)
title.Size = UDim2.new(1, 0, 0, 40)
title.Text = "Akbarshox Hub"
title.TextColor3 = Color3.new(1, 1, 1)
title.Font = Enum.Font.GothamBold
title.TextSize = 20
title.BackgroundTransparency = 1

-- РАДУГА
task.spawn(function()
    while task.wait() do
        title.TextColor3 = Color3.fromHSV(tick() % 5 / 5, 1, 1)
    end
end)

-- ФУНКЦИЯ ДЛЯ КНОПОК
local function createBtn(text, pos, color, callback)
    local btn = Instance.new("TextButton", frame)
    btn.Size = UDim2.new(0.8, 0, 0, 40)
    btn.Position = UDim2.new(0.1, 0, 0, pos)
    btn.BackgroundColor3 = color
    btn.Text = text
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 14
    Instance.new("UICorner", btn)
    btn.MouseButton1Click:Connect(callback)
    return btn
end

-- КНОПКИ
createBtn("Speed 100", 60, Color3.fromRGB(60, 60, 60), function()
    player.Character.Humanoid.WalkSpeed = 100
end)

createBtn("Jump 150", 110, Color3.fromRGB(60, 60, 60), function()
    player.Character.Humanoid.JumpPower = 150
end)

local infJump = false
local jBtn = createBtn("Inf Jump: OFF", 160, Color3.fromRGB(180, 50, 50), function()
    infJump = not infJump
    _G.btnText = infJump and "Inf Jump: ON" or "Inf Jump: OFF"
end)

-- TG ТЕКСТ
local tg = Instance.new("TextLabel", frame)
tg.Size = UDim2.new(1, 0, 0, 30)
tg.Position = UDim2.new(0, 0, 1, -35)
tg.Text = "TG: @UZTUBER_UZ"
tg.TextColor3 = Color3.fromRGB(150, 150, 150)
tg.BackgroundTransparency = 1
tg.TextSize = 12

-- ЛОГИКА ПРЫЖКА
UserInputService.JumpRequest:Connect(function()
    if infJump then player.Character.Humanoid:ChangeState("Jumping") end
end)

-- ЗАКРЫТИЕ
local close = Instance.new("TextButton", frame)
close.Size = UDim2.new(0, 30, 0, 30)
close.Position = UDim2.new(1, -35, 0, 5)
close.Text = "X"
close.TextColor3 = Color3.new(1,0,0)
close.BackgroundTransparency = 1
close.MouseButton1Click:Connect(function() sg:Destroy() end)
