-- [[ AKBARSHOX UI v10.0 - ULTIMATE EDITION ]] --

local Library = {}
local ts = game:GetService("TweenService")
local pgui = game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui")

function Library:CreateWindow(name, animSpeed)
    local ScreenGui = Instance.new("ScreenGui", pgui)
    ScreenGui.Name = "AkbarshoxUI_Premium"
    ScreenGui.ResetOnSpawn = false
    
    -- Настройка плавности (0.8 — золотой стандарт для "дорогого" софта)
    local speed = animSpeed or 0.8
    local info = TweenInfo.new(speed, Enum.EasingStyle.Quart, Enum.EasingDirection.Out)
    
    -- ГЛАВНОЕ ОКНО
    local Main = Instance.new("Frame", ScreenGui)
    Main.Name = "Main"
    Main.BackgroundColor3 = Color3.new(1, 1, 1)
    Main.Position = UDim2.new(0.5, -125, 0.5, -125)
    Main.Size = UDim2.new(0, 0, 0, 0) -- Для анимации появления
    Main.ClipsDescendants = true
    Main.Active = true
    Main.Draggable = true
    Main.BackgroundTransparency = 1
    Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 15)
    
    -- ТОТ САМЫЙ КРОВАВЫЙ ГРАДИЕНТ
    local UIGrad = Instance.new("UIGradient", Main)
    UIGrad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(10, 10, 10)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(220, 0, 0)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(10, 10, 10))
    })
    UIGrad.Rotation = 45
    
    local running = true
    task.spawn(function()
        while running do
            local t = ts:Create(UIGrad, TweenInfo.new(3, Enum.EasingStyle.Linear), {Offset = Vector2.new(1, 1)})
            t:Play()
            t.Completed:Wait()
            UIGrad.Offset = Vector2.new(-1, -1)
        end
    end)

    -- [ ЭФФЕКТ ПОЯВЛЕНИЯ ] --
    ts:Create(Main, info, {
        Size = UDim2.new(0, 260, 0, 260),
        BackgroundTransparency = 0
    }):Play()

    -- ШАПКА
    local TopBar = Instance.new("Frame", Main)
    TopBar.Size = UDim2.new(1, 0, 0, 45)
    TopBar.BackgroundTransparency = 1
    
    local Title = Instance.new("TextLabel", TopBar)
    Title.Size = UDim2.new(0.6, 0, 1, 0)
    Title.Position = UDim2.new(0.08, 0, 0, 0)
    Title.Text = name or "AKBARSHOX UI"
    Title.TextColor3 = Color3.new(1, 1, 1)
    Title.BackgroundTransparency = 1
    Title.Font = Enum.Font.GothamBold
    Title.TextSize = 15
    Title.TextXAlignment = Enum.TextXAlignment.Left

    -- ПУСТОЙ КОНТЕНТ (ДЛЯ БУДУЩИХ ФУНКЦИЙ)
    local Content = Instance.new("Frame", Main)
    Content.Name = "Content"
    Content.Size = UDim2.new(1, 0, 1, -45)
    Content.Position = UDim2.new(0, 0, 0, 45)
    Content.BackgroundTransparency = 1

    -- КНОПКИ УПРАВЛЕНИЯ (БЕЗ ФОНА)
    local function CreateHeadBtn(text, pos, color, callback)
        local btn = Instance.new("TextButton", TopBar)
        btn.Size = UDim2.new(0, 35, 0, 35)
        btn.Position = pos
        btn.Text = text
        btn.TextColor3 = color
        btn.BackgroundTransparency = 1
        btn.Font = Enum.Font.GothamBold
        btn.TextSize = 24
        btn.MouseButton1Click:Connect(callback)
    end

    -- [ ЭФФЕКТ УМИРАНИЯ ] --
    CreateHeadBtn("×", UDim2.new(0.84, 0, 0, 5), Color3.fromRGB(255, 60, 60), function() 
        running = false
        local death = ts:Create(Main, info, {
            Size = UDim2.new(0, 260, 0, 0),
            BackgroundTransparency = 1
        })
        death:Play()
        death.Completed:Connect(function() ScreenGui:Destroy() end)
    end)
    
    -- [ ПЛАВНОЕ СВОРАЧИВАНИЕ ] --
    CreateHeadBtn("-", UDim2.new(0.70, 0, 0, 5), Color3.new(1, 1, 1), function()
        local isMin = Main.Size.Y.Offset < 100
        ts:Create(Main, info, {
            Size = isMin and UDim2.new(0, 260, 0, 260) or UDim2.new(0, 260, 0, 45)
        }):Play()
        Content.Visible = isMin
    end)

    return {} 
end

-- ЗАПУСК (0.8 сек - самая дорогая анимация)
Library:CreateWindow("AKBARSHOX UI", 0.8)
